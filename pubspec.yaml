name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter

  # لازم تكون موجودة
  shared_preferences: ^2.2.2
  uuid: ^4.2.1
  intl: ^0.19.0
  image_picker: ^1.0.7
  image_picker_web: ^3.0.2   # مهم جدًا للويب
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # هنا أضفنا الأصول بشكل صحيح
  assets:
    - assets/images/
    # لو عندك ملفات أو مجلدات أصول أخرى ضيفها هنا بنفس الطريقة
    # مثال:
    # - assets/icons/
    # - assets/fonts/